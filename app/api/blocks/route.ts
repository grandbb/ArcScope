import { NextResponse } from "next/server"; import { alchemyRpc, hexToNumber } from "@/lib/alchemy"; import { CACHE_HEADER } from "@/lib/constants"; import type { Block } from "@/lib/types";
interface RpcBlock { number: string; transactions: unknown[]; gasUsed: string; gasLimit: string; miner: string; timestamp: string }
export async function GET() { try { const result = await alchemyRpc<RpcBlock>("eth_getBlockByNumber", ["latest", false]); const block: Block = { number: hexToNumber(result.number), transactionCount: result.transactions.length, gasUsed: hexToNumber(result.gasUsed), gasLimit: hexToNumber(result.gasLimit), miner: result.miner, timestamp: hexToNumber(result.timestamp) * 1000 }; return NextResponse.json({ data: [block] }, { headers: { "Cache-Control": CACHE_HEADER } }); } catch (error) { return NextResponse.json({ data: [], error: error instanceof Error ? error.message : "Block provider failed" }, { status: 500 }); } }
