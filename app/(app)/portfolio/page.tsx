"use client";
import { useAccount } from "wagmi"; import { Search } from "lucide-react"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { ConnectWalletButton } from "@/components/shared/ConnectWalletButton"; import { PortfolioView } from "@/components/portfolio/PortfolioView"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input";
export default function PortfolioPage() { const { address } = useAccount(); const [query, setQuery] = useState(""); const router = useRouter(); if (address) return <PortfolioView address={address} isOwn/>; return <div className="grid min-h-[70vh] place-items-center"><div className="glass w-full max-w-xl rounded-3xl p-8 text-center md:p-12"><span className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-primary/10 text-primary"><Search/></span><h1 className="mt-5 text-2xl font-bold">Bring your portfolio into focus</h1><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">Connect a wallet for your multi-chain portfolio, or explore any public address and ENS name.</p><div className="mt-6 flex justify-center"><ConnectWalletButton/></div><div className="my-6 flex items-center gap-3 text-xs text-muted-foreground"><i className="h-px flex-1 bg-border"/>OR<i className="h-px flex-1 bg-border"/></div><form className="flex gap-2" onSubmit={(event) => { event.preventDefault(); if (query) router.push(`/portfolio/${query}`); }}><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="0x address or vitalik.eth"/><Button>Explore</Button></form></div></div>; }
