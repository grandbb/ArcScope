"use client";
import { motion, AnimatePresence } from "framer-motion"; import { useLiveBlocks } from "@/hooks/useLiveBlocks"; import { LiveIndicator } from "@/components/shared/LiveIndicator"; import { BlockCard } from "./BlockCard";
export function LiveBlockFeed(){const {blocks,isConnected,error}=useLiveBlocks(); const rows=blocks.slice(0,10); return <article className="glass overflow-hidden rounded-2xl"><header className="flex items-center justify-between border-b bg-gradient-to-r from-primary/[.07] to-transparent p-5"><div><h2 className="font-bold tracking-tight">Live Arc blocks</h2><p className="mt-0.5 text-xs text-muted-foreground">Sub-second Arc Mainnet finality</p></div><LiveIndicator connected={isConnected}/></header><div className="divide-y divide-border/70">{error && <p role="alert" className="p-5 text-sm text-muted-foreground">{error.message}</p>}{!rows.length && !error && <p className="p-5 text-sm text-muted-foreground">Connecting to Arc Mainnet...</p>}<AnimatePresence initial={false}>{rows.map((block)=><motion.div key={block.number} initial={{opacity:0,y:-16}} animate={{opacity:1,y:0}} exit={{opacity:0}}><BlockCard block={block}/></motion.div>)}</AnimatePresence></div></article>;}
