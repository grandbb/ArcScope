"use client";
import { useWhaleAlerts } from "@/hooks/useWhaleAlerts"; import { WhaleAlertCard } from "./WhaleAlertCard"; import { mockWhales } from "@/lib/mock-data"; import { EmptyState } from "@/components/shared/EmptyState";
export function WhaleAlerts(){const query=useWhaleAlerts();const alerts=query.data??mockWhales;return <article className="glass overflow-hidden rounded-2xl"><header className="border-b bg-gradient-to-r from-[hsl(var(--chart-3)/.07)] to-transparent p-5"><h2 className="font-bold tracking-tight">Arc whale alerts</h2><p className="mt-0.5 text-xs text-muted-foreground">Native transfers above 100,000 USDC</p></header><div className="divide-y divide-border/70">{alerts.length?alerts.slice(0,10).map((alert)=><WhaleAlertCard key={alert.hash} alert={alert}/>):<EmptyState title="No whale activity" description="Large transfers will appear here in real time."/>}</div></article>;}
