import { ArrowUpRight } from "lucide-react"; import type { WhaleTransaction } from "@/lib/types"; import { formatAddress, timeAgo } from "@/lib/formatters";
export function WhaleAlertCard({alert}:{alert:WhaleTransaction}){return <div className="p-4"><div className="flex items-center justify-between"><span className="rounded-lg bg-amber-500/10 px-2 py-1 font-mono text-xs text-amber-500">{alert.value.toLocaleString()} {alert.symbol}</span><span className="text-[10px] text-muted-foreground">{timeAgo(alert.timestamp)}</span></div><div className="mt-3 flex items-center gap-2 text-xs"><span className="font-medium">{alert.fromLabel??formatAddress(alert.from)}</span><ArrowUpRight className="h-3.5 w-3.5 text-muted-foreground"/><span className="font-medium">{alert.toLabel??formatAddress(alert.to)}</span></div></div>;}
