import { Box } from "lucide-react"; import type { Block } from "@/lib/types"; import { formatAddress, timeAgo } from "@/lib/formatters";
export function BlockCard({block}:{block:Block}){const gas=block.gasLimit?Math.round(block.gasUsed/block.gasLimit*100):0;return <div className="grid grid-cols-[1fr_auto_auto] items-center gap-5 p-4 text-sm hover:bg-muted/40"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-primary/10 text-primary"><Box className="h-4 w-4"/></span><div><p className="font-mono font-semibold">#{block.number.toLocaleString()}</p><p className="text-xs text-muted-foreground">{timeAgo(block.timestamp)}</p></div></div><div><p className="font-mono">{block.transactionCount}</p><p className="text-[10px] text-muted-foreground">TXS</p></div><div className="w-20"><p className="font-mono text-xs">{gas}% · {formatAddress(block.miner)}</p><div className="mt-1 h-1 rounded bg-muted"><i className="block h-1 rounded bg-accent" style={{width:`${gas}%`}}/></div></div></div>;}
