import type { LucideIcon } from "lucide-react"; import { Card, CardContent } from "@/components/ui/card";
export function MetricCard({ label, value, change, icon: Icon }: { label: string; value: string; change?: number; icon?: LucideIcon }) { return <Card><CardContent className="p-4"><div className="flex items-start justify-between"><p className="text-xs text-muted-foreground">{label}</p>{Icon && <Icon className="h-4 w-4 text-primary"/>}</div><p className="mt-2 font-mono text-2xl font-bold tabular-nums">{value}</p>{change !== undefined && <p className={change >= 0 ? "mt-2 text-xs text-emerald-500" : "mt-2 text-xs text-red-500"}>{change >= 0 ? "+" : ""}{change.toFixed(2)}%</p>}</CardContent></Card>; }
