"use client";
import React from "react"; import { AlertTriangle } from "lucide-react"; import { Button } from "@/components/ui/button";
interface State { error: Error | null }
export class ErrorBoundary extends React.Component<{ children: React.ReactNode }, State> { state: State = { error: null }; static getDerivedStateFromError(error: Error): State { return { error }; } render() { if (this.state.error) return <div className="grid min-h-56 place-items-center rounded-xl border bg-card p-8 text-center"><div><AlertTriangle className="mx-auto text-amber-500"/><h2 className="mt-3 font-semibold">This section could not be loaded</h2><p className="mt-1 text-sm text-muted-foreground">{this.state.error.message}</p><Button className="mt-4" onClick={() => this.setState({ error: null })}>Try again</Button></div></div>; return this.props.children; } }
