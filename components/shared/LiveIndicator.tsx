import { cn } from "@/lib/utils";
export function LiveIndicator({ connected = true, className }: { connected?: boolean; className?: string }) { return <span className={cn("inline-flex items-center gap-2 text-[10px] font-bold uppercase", connected ? "text-emerald-500" : "text-amber-500", className)}><i className={cn("h-2 w-2 rounded-full", connected ? "animate-pulse bg-emerald-500" : "bg-amber-500")}/>{connected ? "Live" : "Reconnecting"}</span>; }
