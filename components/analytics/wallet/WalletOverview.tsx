import { CalendarClock, Coins, History, MousePointerClick, ReceiptText } from "lucide-react"; import type { WalletAnalyticsData } from "@/hooks/useWalletAnalytics"; import { MetricCard } from "@/components/shared/MetricCard";
export function WalletOverview({ wallet }: { wallet: WalletAnalyticsData }) { return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><MetricCard label="ETH balance" value={`${wallet.balance.toFixed(2)} ETH`} icon={Coins}/><MetricCard label="Total transactions" value={wallet.txCount.toLocaleString()} icon={ReceiptText}/><MetricCard label="First transaction" value={wallet.firstTx} icon={History}/><MetricCard label="Last active" value="Today" icon={CalendarClock}/><MetricCard label="Unique contracts" value={wallet.uniqueContracts.toLocaleString()} icon={MousePointerClick}/></div>; }
