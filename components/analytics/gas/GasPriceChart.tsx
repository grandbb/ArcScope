"use client";
import { Line, LineChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { GasData } from "@/lib/types"; import { useTimeRange } from "@/hooks/useTimeRange"; import { TimeRangeSelector } from "@/components/shared/TimeRangeSelector";
export function GasPriceChart({ data }: { data: GasData["history"] }) { const { range, setRange } = useTimeRange("24H"); return <section className="glass rounded-2xl p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Gas price history</h2><p className="text-xs text-muted-foreground">Fee tiers over time</p></div><TimeRangeSelector value={range} onChange={setRange} options={["1H","6H","24H","7D"]}/></div><div className="mt-5 h-80"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid strokeDasharray="3 3" opacity={.12}/><XAxis dataKey="time" axisLine={false} tickLine={false} fontSize={11}/><YAxis unit=" g" axisLine={false} tickLine={false} fontSize={11}/><Tooltip/><Line type="monotone" dataKey="slow" stroke="#06b6d4" dot={false}/><Line type="monotone" dataKey="standard" stroke="#8b5cf6" dot={false} strokeWidth={2}/><Line type="monotone" dataKey="fast" stroke="#f59e0b" dot={false}/></LineChart></ResponsiveContainer></div></section>; }
