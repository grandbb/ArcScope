import type { NetworkStat } from "@/hooks/useNetworkStats"; import { formatNumber } from "@/lib/formatters";
export function NetworkStats({ networks }: { networks: NetworkStat[] }) { return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{networks.map((network)=><article key={network.name} className="glass rounded-2xl p-5"><h3 className="font-semibold">{network.name}</h3><div className="mt-4 grid grid-cols-2 gap-4">{[["TPS",network.tps],["Block time",`${network.blockTime}s`],["Gas",`${network.gas} Gwei`],["Daily txs",formatNumber(network.dailyTransactions)]].map(([label,value])=><div key={String(label)}><p className="text-[10px] uppercase text-muted-foreground">{label}</p><p className="mt-1 font-mono text-lg">{value}</p></div>)}</div></article>)}</div>; }
