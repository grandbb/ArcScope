"use client";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { TokenAnalyticsData } from "@/hooks/useTokenAnalytics";
export function TransferVolume({ candles }: { candles: TokenAnalyticsData["candles"] }) { const data = candles.slice(-30).map((item,index)=>({ day: index+1, volume: item.volume })); return <section className="glass rounded-2xl p-4"><h2 className="font-semibold">Transfer volume</h2><p className="text-xs text-muted-foreground">Daily token transfer volume</p><div className="mt-4 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3" opacity={.12}/><XAxis dataKey="day" fontSize={10}/><YAxis tickFormatter={(v:number)=>`${v/1e6}m`} fontSize={10}/><Tooltip/><Bar dataKey="volume" fill="#06b6d4" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div></section>; }
