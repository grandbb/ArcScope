import { CopyButton } from "@/components/shared/CopyButton"; import { AddToWatchlist } from "@/components/watchlist/AddToWatchlist"; import { MetricCard } from "@/components/shared/MetricCard"; import { CircleDollarSign, Coins, Landmark, Waves } from "lucide-react"; import { formatAddress, formatCurrency, formatNumber } from "@/lib/formatters"; import type { TokenAnalyticsData } from "@/hooks/useTokenAnalytics";
export function TokenOverview({ token }: { token: TokenAnalyticsData }) { return <div className="space-y-5"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-4"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-primary/10 font-bold text-primary">{token.symbol}</span><div><h1 className="text-2xl font-bold">{token.name} <span className="text-muted-foreground">{token.symbol}</span></h1><div className="flex items-center text-xs text-muted-foreground"><span className="font-mono">{formatAddress(token.address,8)}</span><CopyButton value={token.address}/></div></div></div><AddToWatchlist id={token.address} address={token.address} label={token.symbol} type="token"/></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Price" value={formatCurrency(token.price)} icon={CircleDollarSign}/><MetricCard label="Market cap" value={`$${formatNumber(token.marketCap)}`} icon={Landmark}/><MetricCard label="24h volume" value={`$${formatNumber(token.volume24h)}`} icon={Waves}/><MetricCard label="Total supply" value={formatNumber(token.supply)} icon={Coins}/></div></div>; }
