"use client";
import { useState } from "react"; import type { NFTAsset } from "@/lib/types"; import { NFTCard } from "./NFTCard"; import { NFTDetailModal } from "./NFTDetailModal"; import { EmptyState } from "@/components/shared/EmptyState";
export function NFTGallery({ nfts }: { nfts: NFTAsset[] }) { const [selected, setSelected] = useState<NFTAsset>(); if (!nfts.length) return <EmptyState title="No NFTs found" description="This account does not hold any NFTs on the selected networks."/>; return <><div className="mb-4 flex gap-2"><select className="h-9 rounded-lg border bg-card px-3 text-sm"><option>All collections</option></select><select className="h-9 rounded-lg border bg-card px-3 text-sm"><option>Recently acquired</option><option>Name</option><option>Floor price</option></select></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{nfts.map((nft) => <NFTCard key={nft.id} nft={nft} onClick={() => setSelected(nft)}/>)}</div><NFTDetailModal nft={selected} open={Boolean(selected)} onOpenChange={(open) => !open && setSelected(undefined)}/></>; }
