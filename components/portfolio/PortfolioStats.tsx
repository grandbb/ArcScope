import { Coins, Image, ReceiptText, TrendingUp } from "lucide-react"; import { MetricCard } from "@/components/shared/MetricCard"; import type { Portfolio } from "@/lib/types"; import { formatCurrency, formatNumber } from "@/lib/formatters";
export function PortfolioStats({ portfolio }: { portfolio: Portfolio }) { return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Net worth" value={formatCurrency(portfolio.totalValue)} change={portfolio.change24h} icon={TrendingUp}/><MetricCard label="Tokens" value={String(portfolio.tokenCount)} icon={Coins}/><MetricCard label="NFTs" value={String(portfolio.nftCount)} icon={Image}/><MetricCard label="Transactions" value={formatNumber(portfolio.transactionCount)} icon={ReceiptText}/></div>; }
