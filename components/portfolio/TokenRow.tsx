import type { TokenBalance } from "@/lib/types"; import { formatCurrency, formatNumber } from "@/lib/formatters"; import { TableCell, TableRow } from "@/components/ui/table";
export function TokenRow({ token }: { token: TokenBalance }) { return <TableRow><TableCell><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-primary/10 font-mono text-xs text-primary">{token.symbol.slice(0,3)}</span><div><p className="font-medium">{token.name}</p><p className="text-xs text-muted-foreground">{token.symbol}</p></div></div></TableCell><TableCell className="font-mono">{formatNumber(token.balance)}</TableCell><TableCell className="font-mono">{formatCurrency(token.price)}</TableCell><TableCell className="font-mono font-medium">{formatCurrency(token.value)}</TableCell><TableCell className={`font-mono ${token.change24h >= 0 ? "text-emerald-500" : "text-red-500"}`}>{token.change24h >= 0 ? "+" : ""}{token.change24h.toFixed(2)}%</TableCell><TableCell><div className="flex items-center gap-2"><span className="w-12 font-mono text-xs">{token.allocation.toFixed(1)}%</span><i className="h-1.5 w-16 rounded-full bg-muted"><i className="block h-full rounded-full bg-primary" style={{ width: `${Math.min(100, token.allocation)}%` }}/></i></div></TableCell></TableRow>; }
