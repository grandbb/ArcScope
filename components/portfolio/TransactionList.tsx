import type { Transaction } from "@/lib/types"; import { Table, TableBody, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { EmptyState } from "@/components/shared/EmptyState"; import { TransactionRow } from "./TransactionRow";
export function TransactionList({ transactions }: { transactions: Transaction[] }) { return <section className="glass overflow-hidden rounded-2xl"><header className="border-b p-4"><h2 className="font-semibold">Transaction history</h2><p className="text-xs text-muted-foreground">Most recent on-chain activity</p></header>{transactions.length ? <div className="overflow-x-auto"><Table className="min-w-[800px]"><TableHeader><TableRow><TableHead></TableHead><TableHead>Type</TableHead><TableHead>Counterparty</TableHead><TableHead>Value</TableHead><TableHead>Gas fee</TableHead><TableHead>Time</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{transactions.map((transaction) => <TransactionRow key={transaction.hash} transaction={transaction}/>)}</TableBody></Table></div> : <EmptyState title="No activity found" description="Transactions will appear here after this account interacts on-chain."/>}</section>; }
