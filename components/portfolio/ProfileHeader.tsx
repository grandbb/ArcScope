import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"; import { Button } from "@/components/ui/button"; import { Download } from "lucide-react"; import { AnimatedNumber } from "@/components/shared/AnimatedNumber"; import { ShareButton } from "@/components/shared/ShareButton"; import { WalletAddress } from "./WalletAddress";
export function ProfileHeader({ address, name = "On-chain account", avatar, totalValue, actions }: { address: string; name?: string; avatar?: string; totalValue: number; actions?: React.ReactNode }) { return <section className="glass rounded-2xl p-5 md:p-6"><div className="flex flex-col justify-between gap-5 lg:flex-row lg:items-center"><div className="flex items-center gap-4"><Avatar className="h-16 w-16"><AvatarImage src={avatar}/><AvatarFallback className="bg-primary/15 text-lg text-primary">{name.slice(0,2).toUpperCase()}</AvatarFallback></Avatar><div><h1 className="text-2xl font-bold">{name}</h1><WalletAddress address={address}/><p className="text-xs text-muted-foreground">Active since Jan 2021 · 5.6 years</p></div></div><div className="lg:text-right"><p className="text-xs uppercase tracking-wider text-muted-foreground">Total portfolio value</p><p className="mt-1 text-3xl font-bold"><AnimatedNumber value={totalValue} prefix="$" decimals={2}/></p><div className="mt-3 flex flex-wrap gap-2 lg:justify-end"><ShareButton/><Button variant="outline"><Download/>Export</Button>{actions}</div></div></div></section>; }
