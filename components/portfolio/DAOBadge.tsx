import type { DAO } from "@/lib/types"; import { ExternalLink } from "lucide-react";
export function DAOBadge({ dao }: { dao: DAO }) { return <a href={dao.governanceUrl} target="_blank" rel="noreferrer" className="glass rounded-2xl p-5 transition hover:-translate-y-1"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-primary/10 font-bold text-primary">{dao.symbol}</span><ExternalLink className="h-4 w-4 text-muted-foreground"/></div><h3 className="mt-4 font-semibold">{dao.name}</h3><p className="mt-1 font-mono">{dao.balance.toLocaleString()} {dao.symbol}</p><p className="mt-3 text-xs text-muted-foreground">Voting power <span className="float-right font-mono text-foreground">{dao.votingPower}%</span></p></a>; }
