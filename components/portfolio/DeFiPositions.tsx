import type { DefiPosition } from "@/lib/types"; import { formatCurrency } from "@/lib/formatters"; import { EmptyState } from "@/components/shared/EmptyState"; import { DeFiPositionCard } from "./DeFiPositionCard";
export function DeFiPositions({ positions }: { positions: DefiPosition[] }) { const total = positions.reduce((sum, item) => sum + item.value, 0); if (!positions.length) return <EmptyState title="No DeFi positions" description="Supported positions from Aave, Compound, Lido, Uniswap, Curve and Convex appear here."/>; return <div className="space-y-4"><div className="glass rounded-2xl p-5"><p className="text-xs text-muted-foreground">Total DeFi value</p><p className="mt-2 font-mono text-3xl font-bold">{formatCurrency(total)}</p></div><div className="grid gap-4 lg:grid-cols-2">{positions.map((position)=><DeFiPositionCard key={position.id} position={position}/>)}</div></div>; }
