"use client";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import { useTimeRange } from "@/hooks/useTimeRange"; import { TimeRangeSelector } from "@/components/shared/TimeRangeSelector";
const data = Array.from({ length: 30 }, (_, index) => ({ day: `Aug ${index + 1}`, value: 232000 + index * 1400 + Math.sin(index / 2) * 16000 }));
export function NetWorthChart() { const { range, setRange } = useTimeRange("30D"); return <section className="glass rounded-2xl p-4"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Net worth</h2><p className="text-xs text-muted-foreground">Portfolio value over time</p></div><TimeRangeSelector value={range} onChange={setRange}/></div><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="net" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#8b5cf6" stopOpacity={.45}/><stop offset="1" stopColor="#8b5cf6" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" opacity={.12}/><XAxis dataKey="day" hide/><YAxis tickFormatter={(v: number) => `$${v/1000}k`} axisLine={false} tickLine={false} fontSize={11}/><Tooltip formatter={(value: number) => [`$${value.toLocaleString()}`, "Value"]}/><Area type="monotone" dataKey="value" stroke="#8b5cf6" fill="url(#net)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div></section>; }
