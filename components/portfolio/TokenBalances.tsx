import type { TokenBalance } from "@/lib/types"; import { Table, TableBody, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { EmptyState } from "@/components/shared/EmptyState"; import { TokenRow } from "./TokenRow";
export function TokenBalances({ tokens }: { tokens: TokenBalance[] }) { return <section className="glass overflow-hidden rounded-2xl"><header className="border-b p-4"><h2 className="font-semibold">Token balances</h2><p className="text-xs text-muted-foreground">Assets aggregated across supported networks</p></header>{tokens.length ? <div className="overflow-x-auto"><Table className="min-w-[760px]"><TableHeader><TableRow><TableHead>Token</TableHead><TableHead>Balance</TableHead><TableHead>Price</TableHead><TableHead>Value</TableHead><TableHead>24h</TableHead><TableHead>Allocation</TableHead></TableRow></TableHeader><TableBody>{tokens.map((token) => <TokenRow key={token.address} token={token}/>)}</TableBody></Table></div> : <EmptyState title="No token balances" description="Tokens held by this account will appear here."/>}</section>; }
