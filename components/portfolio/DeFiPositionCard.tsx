import type { DefiPosition } from "@/lib/types"; import { Badge } from "@/components/ui/badge"; import { formatCurrency } from "@/lib/formatters";
export function DeFiPositionCard({ position }: { position: DefiPosition }) { return <article className="glass rounded-2xl p-5"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary/10 font-bold text-primary">{position.protocol[0]}</span><div><h3 className="font-semibold">{position.protocol}</h3><p className="text-xs text-muted-foreground">{position.type}</p></div></div><Badge className="bg-emerald-500/10 text-emerald-500">{position.apy}% APY</Badge></div><div className="mt-5 grid grid-cols-3 gap-3"><div><p className="text-[10px] text-muted-foreground">DEPOSITED</p><p className="mt-1 font-mono">{formatCurrency(position.deposited)}</p></div><div><p className="text-[10px] text-muted-foreground">CURRENT</p><p className="mt-1 font-mono">{formatCurrency(position.value)}</p></div><div><p className="text-[10px] text-muted-foreground">PNL</p><p className={`mt-1 font-mono ${position.pnl >= 0 ? "text-emerald-500" : "text-red-500"}`}>{formatCurrency(position.pnl)}</p></div></div></article>; }
