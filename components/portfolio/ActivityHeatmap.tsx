export function ActivityHeatmap() { const cells = Array.from({ length: 364 }, (_, index) => (index * 7 + index % 5) % 6); return <section className="glass overflow-hidden rounded-2xl p-4"><h2 className="font-semibold">Activity heatmap</h2><p className="text-xs text-muted-foreground">365 days of wallet activity</p><div className="mt-5 overflow-x-auto pb-2"><div className="grid w-[760px] grid-flow-col grid-rows-7 gap-1">{cells.map((level,index)=><i key={index} title={`${level} transactions`} className="h-3 w-3 rounded-sm" style={{ backgroundColor: level === 0 ? "hsl(var(--muted))" : `hsl(var(--primary) / ${.18 + level * .14})` }}/>)}</div></div></section>; }
