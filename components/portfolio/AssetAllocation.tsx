"use client";
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts"; import { CHART_COLORS } from "@/lib/colors"; import type { TokenBalance } from "@/lib/types";
export function AssetAllocation({ tokens }: { tokens: TokenBalance[] }) { return <section className="glass rounded-2xl p-4"><h2 className="font-semibold">Asset allocation</h2><p className="text-xs text-muted-foreground">By current market value</p><div className="h-56"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={tokens} dataKey="value" nameKey="symbol" innerRadius={60} outerRadius={86} paddingAngle={3}>{tokens.map((token,index) => <Cell key={token.address} fill={CHART_COLORS[index % CHART_COLORS.length]}/>)}</Pie><Tooltip formatter={(value: number) => `$${value.toLocaleString()}`}/></PieChart></ResponsiveContainer></div><div className="grid grid-cols-2 gap-2">{tokens.map((token,index)=><div key={token.address} className="flex items-center gap-2 text-xs"><i className="h-2 w-2 rounded-full" style={{backgroundColor: CHART_COLORS[index % CHART_COLORS.length]}}/>{token.symbol}<span className="ml-auto font-mono text-muted-foreground">{token.allocation.toFixed(1)}%</span></div>)}</div></section>; }
