"use client";
import Image from "next/image"; import type { NFTAsset } from "@/lib/types"; import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
export function NFTDetailModal({ nft, open, onOpenChange }: { nft?: NFTAsset; open: boolean; onOpenChange: (open: boolean) => void }) { if (!nft) return null; return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-3xl"><div className="grid gap-6 md:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-xl bg-muted"><Image src={nft.image} alt={nft.name} fill className="object-cover"/></div><div><DialogHeader><p className="text-xs text-primary">{nft.collection}</p><DialogTitle>{nft.name}</DialogTitle></DialogHeader><p className="mt-4 text-sm text-muted-foreground">{nft.description}</p><h3 className="mt-6 text-sm font-semibold">Traits</h3><div className="mt-2 grid grid-cols-2 gap-2">{nft.traits.map((trait) => <div key={trait.traitType} className="rounded-lg border bg-muted/30 p-3"><p className="text-[10px] uppercase text-muted-foreground">{trait.traitType}</p><p className="mt-1 text-sm font-medium">{trait.value}</p></div>)}</div></div></div></DialogContent></Dialog>; }
