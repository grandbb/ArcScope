"use client";
import { useWatchlistStore } from "@/stores/useWatchlistStore"; import type { WatchlistItem } from "@/lib/types"; import { Button } from "@/components/ui/button"; import { Star } from "lucide-react";
export function AddToWatchlist({ id, address, label, type, children }: Pick<WatchlistItem, "id"|"address"|"label"|"type"> & { children?: React.ReactNode }) { const items = useWatchlistStore((state) => state.items); const add = useWatchlistStore((state) => state.add); const remove = useWatchlistStore((state) => state.remove); const active = items.some((item) => item.id === id); return <Button variant="outline" onClick={() => active ? remove(id) : add({ id, address, label, type, chain: "arc" })} className={active ? "text-amber-500" : ""}>{children ?? <Star className={active ? "fill-current" : ""}/>}<span>{active ? "Saved" : "Watchlist"}</span></Button>; }
