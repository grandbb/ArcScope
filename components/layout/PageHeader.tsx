import Link from "next/link"; import { ChevronRight } from "lucide-react";
export function PageHeader({ title, description, breadcrumbs = [], actions, eyebrow }: { title: string; description?: string; breadcrumbs?: Array<{ label: string; href?: string }>; actions?: React.ReactNode; eyebrow?: string }) { return <div className="relative flex flex-wrap items-end justify-between gap-4"><div>{breadcrumbs.length > 0 && <div className="mb-2 flex items-center gap-1 text-xs text-muted-foreground">{breadcrumbs.map((crumb, index) => <span key={crumb.label} className="flex items-center gap-1">{index > 0 && <ChevronRight className="h-3 w-3"/>}{crumb.href ? <Link href={crumb.href}>{crumb.label}</Link> : crumb.label}</span>)}</div>}{eyebrow && <div className="mb-2 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.2em] text-primary"><span className="brand-gradient h-px w-7" />{eyebrow}</div>}<h1 className="text-2xl font-black tracking-[-.035em] sm:text-[28px]">{title}</h1>{description && <p className="mt-1.5 max-w-2xl text-sm leading-relaxed text-muted-foreground">{description}</p>}</div>{actions}</div>; }
