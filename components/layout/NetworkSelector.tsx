"use client";
import { CHAINS } from "@/lib/chains"; import { useSettingsStore } from "@/stores/useSettingsStore"; import type { ChainSlug } from "@/lib/types";
export function NetworkSelector() { const chain = useSettingsStore((state) => state.chain); const setChain = useSettingsStore((state) => state.setChain); return <label className="hidden items-center gap-2 rounded-xl border bg-card px-3 sm:flex"><i className="h-2 w-2 rounded-full" style={{ backgroundColor: CHAINS.find((item) => item.slug === chain)?.color }}/><select aria-label="Network" value={chain} onChange={(event) => setChain(event.target.value as ChainSlug)} className="h-9 bg-transparent text-xs font-medium outline-none">{CHAINS.map((item) => <option key={item.id} value={item.slug}>{item.name}</option>)}</select></label>; }
